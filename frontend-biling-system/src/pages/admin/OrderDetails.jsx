import AdminLayout from "../../components/admin/AdminLayout";

const OrderDetails = () => {
  return (
    <AdminLayout>
      <h2>Order Details</h2>
      <p>Fetched via Order ID (handled in OrderManagement)</p>
    </AdminLayout>
  );
};

export default OrderDetails;
